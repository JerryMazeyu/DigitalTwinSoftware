# AGENTS.md

本文件为本仓库中所有开发智能体的项目级说明。后续在本项目内进行需求拆解、原型开发、前后端实现、数据模拟、接口联调、测试和文档编写时，均应优先遵循本文件。

## 项目背景

本项目面向卷对卷工业镀膜场景，目标是建设一个用于实时展示镀膜设备、工艺参数、图像采集、算法推理结果和趋势预警的数字孪生可视化平台。

现有工业链路如下：

1. 最底层为卷对卷镀膜机。
2. 镀膜机上安装智能线扫相机。
3. 线扫相机通过网线将实时图像传入工控机。
4. 本项目相关软件部署在工控机环境中。
5. 工控机与算法服务器相连，将采集到的图像发送到服务器。
6. 服务器运行自研异常检测算法和褶皱趋势预测算法，并返回推理结果。
7. 推理结果回传至工控机。
8. 工控机可根据结果反向控制镀膜机，实现检测、预警、辅助决策和闭环控制。

数字孪生平台的职责不是替代底层控制系统，而是把上述链路中“设备状态、工艺参数、实时图像、算法结果、趋势预测、告警事件、控制反馈”集中、实时、直观地展示出来。

## 核心目标

平台应优先服务以下目标：

- 实时展示镀膜机运行状态和关键工艺参数。
- 实时展示线扫相机采集到的图片或图片流。
- 展示异常检测结果，包括异物、残留物、划痕、污染、破损、褶皱等可扩展缺陷类型。
- 展示褶皱趋势预测结果，突出当前褶皱是否可能扩散、扩散方向、风险等级和预测时间窗。
- 展示算法服务器、工控机、相机、镀膜机之间的数据链路状态。
- 展示告警、处置建议、控制指令下发状态和设备反馈。
- 以数字孪生驾驶舱形式呈现，视觉上具有工业智能、高端制造、实时监控的质感。
- 可以配置各项数据流的来源，支持各种协议传输数据。

## 产品形态

首屏应是可直接用于生产监控的数字孪生工作台，不要做营销型落地页。

推荐首屏信息结构：

- 顶部：产线名称、当前配方、批次号、运行状态、系统时间、连接状态、总告警数。
- 左侧：镀膜机数字孪生示意图，可采用高质量 2D/3D 示意，不要求精确 CAD 建模，但需要表现放卷、镀膜、检测、收卷等关键段。
- 中央：实时图像展示区，显示线扫图像、缺陷框、分割掩膜、风险热力层或最新帧队列。
- 右侧：算法结果面板，展示异常检测类别、置信度、褶皱扩散概率、风险等级、处置建议。
- 底部：趋势曲线和事件时间线，包括张力、速度、温度、真空度、褶皱风险、缺陷数量等。

视觉表达可以是示意化的，但数据组织必须真实可信。不要只做静态大屏背景图；所有核心区域都应能由结构化数据驱动。

## 系统边界

开发时将系统划分为以下边界：

- 设备层：镀膜机、相机、传感器、执行机构。
- 工控机层：图像接收、缓存、数据转发、控制指令网关、与设备的工业协议交互。
- 算法服务层：异常检测、褶皱趋势预测、结果解释、模型版本管理。
- 数字孪生平台层：状态聚合、实时可视化、告警展示、趋势分析、操作审计。

数字孪生平台不应直接绕过工控机控制镀膜机。任何控制相关能力都必须通过明确的后端网关、权限校验、确认机制和审计记录。

## 数据流

标准数据流应按以下方向建模：

```text
线扫相机 -> 工控机 -> 算法服务器 -> 工控机 -> 数字孪生平台
                                      -> 镀膜机控制反馈
```

平台可以从工控机、后端聚合服务或算法服务获取数据，但前端代码不应直接依赖某个临时脚本或算法内部目录。应通过稳定接口或适配器层读取数据。

实时数据建议使用 WebSocket、Server-Sent Events 或本地消息队列适配。图片资源应使用 URL、对象存储路径、本地静态服务路径或二进制流引用，不要把大图直接塞进普通 JSON 状态中。

## 建议领域模型

后续实现中应优先围绕以下领域对象建模：

- `MachineStatus`：运行状态、线速度、张力、温度、真空度、功率、配方、卷材信息、设备告警。
- `CameraFrame`：帧 ID、采集时间、相机 ID、产线位置、图片地址、帧宽高、采集质量。
- `DetectionResult`：帧 ID、缺陷类别、置信度、位置框、掩膜、严重程度、算法版本。
- `WrinklePrediction`：帧 ID、褶皱风险分数、扩散概率、预测时间窗、扩散方向、建议动作。
- `AlarmEvent`：事件 ID、等级、来源、描述、发生时间、确认状态、恢复时间。
- `ControlAction`：建议动作、下发状态、操作人、确认时间、设备响应、审计记录。
- `SystemHealth`：工控机、算法服务器、相机、网络、模型服务的在线状态和延迟。

如需添加字段，应优先保持事件可追踪、时间戳清晰、单位明确、来源明确。

## 实时性要求

平台应体现“实时生产监控”的体验：

- 新图片、新检测结果和新预测结果应能连续进入界面。
- UI 不应因单帧图片加载失败或算法服务延迟而整体卡死。
- 所有实时数据都应显示采集时间或结果时间，避免用户误判旧数据为实时数据。
- 对网络断开、算法超时、相机离线、设备停机等状态提供清晰展示。
- 对高频图像流做节流、抽样、缓存上限或虚拟列表处理，避免浏览器内存持续增长。

## 异常与趋势展示

异常检测展示应同时覆盖“当前是否有问题”和“问题在哪里”：

- 缺陷类别、置信度、严重等级。
- 图片上的框选、轮廓、掩膜或热力层。
- 缺陷数量和近一段时间变化趋势。
- 与工艺参数之间的时间关联。

褶皱趋势预测展示应覆盖“是否会扩散”和“扩散风险多大”：

- 当前褶皱风险分数。
- 未来一段时间或后续镀膜区间的扩散概率。
- 风险等级：正常、关注、预警、严重。
- 扩散方向、影响范围或示意箭头。
- 建议动作，如降低线速度、调整张力、检查卷材状态等。建议动作默认仅作为辅助决策，不自动下发控制。

## 控制与安全

涉及反向控制镀膜机时，必须遵守以下原则：

- 默认只展示建议，不自动执行控制。
- 控制动作必须经过权限校验和操作确认。
- 每条控制指令必须记录发起来源、参数、时间、操作人、设备响应。
- 前端不得伪造控制成功状态，必须以后端或工控机确认为准。
- 调试模式、演示模式和真实生产模式必须明显区分。
- 无法确认设备状态时，应降级为只读监控模式。

## 开发原则

如果仓库尚未形成固定技术栈，前端原型优先采用 TypeScript + React + Vite；图表可采用 ECharts；复杂 3D 场景可采用 Three.js；实时数据可先使用 mock adapter，再替换为真实接口。

无论采用何种技术栈，都应遵循以下原则：

- 先定义数据结构和适配器，再绑定 UI。
- 模拟数据、真实接口、算法结果解析应分层，不要混在组件里。
- 数字孪生示意图应由设备状态驱动，例如运行、停机、告警、离线、控制中。
- 页面组件应围绕生产工作流组织，而不是围绕视觉装饰组织。
- 保持演示效果完整：即使真实设备未接入，也要有可信的模拟数据流。
- 告警颜色、风险等级、单位和时间格式应保持全局一致。
- 对关键路径保留日志和错误提示，便于现场联调。

## UI 设计约束

平台应呈现工业智能可视化风格，但不要牺牲可读性：

- 使用克制、专业的深色或中性色底色，搭配少量高对比告警色和状态色。
- 不要让界面只有单一蓝紫渐变或纯装饰大屏效果。
- 核心数据必须清晰可读，避免过多发光、粒子、光晕遮挡信息。
- 设备示意、图像、趋势曲线、告警事件应同时可见，避免信息分散到过多页面。
- 按钮、图标、图例、状态标识应语义明确。
- 移动端不是首要场景，但页面在较小屏幕下不应出现严重重叠。

## 测试与验证

后续开发完成后，至少验证以下内容：

- 模拟数据流能持续刷新图像、参数、算法结果和告警。
- 相机离线、服务器离线、算法超时、图片加载失败时界面能降级展示。
- 缺陷框、掩膜或热力层能正确对齐图片。
- 趋势曲线不会因为高频数据无限增长导致卡顿。
- 控制建议和真实控制动作在界面上有明显区别。
- 关键数据对象有单元测试或等价的结构校验。
- 前端界面需通过浏览器实际查看，确认首屏布局没有文本重叠、空白主视图或不可读状态。

## 文档维护

当项目发生以下变化时，应同步更新本文件或相关设计文档：

- 数据链路变化，例如算法服务接入方式改变。
- 缺陷类型、风险等级、模型输出格式变化。
- 工控机与服务器通信协议变化。
- 增加真实控制能力或权限系统。
- UI 信息架构发生较大调整。
- 项目技术栈和运行方式确定后。

## 临时文件与建模产物

凡是建模脚本（Blender、CAD 转换、材质烘焙、相机标定）、渲染验证产物（PNG、JSON、MP4）、一次性巡检脚本（`inspect_*`、`verify_*`、`debug_*`、`_tmp`、`_verify_*`）等都属于临时产物，不应提交到仓库。

约定：

- **存放位置**：所有这类文件必须放在 `artifacts/<agent-or-topic>/` 子目录下。当前已有的示例：`artifacts/codex/`（之前建模探索产生的临时脚本、验证截图与 JSON 已经全部归档于此）。
- **不要污染主目录**：任何名为 `*.py` 的脚本如果是一次性的，请放进 `artifacts/<topic>/`，不要直接放在 `tools/`（`tools/` 只放**会被复用**的脚本，例如 `tools/smoke_plc_api.mjs`、`tools/generate_plc_sensor_map.py`）。
- **不要污染 docs/**：建模规划、规格等需要长期保留的文档放在 `docs/`；一次性渲染日志、临时检查清单等放在 `artifacts/<topic>/`。
- **gitignore**：`artifacts/` 已经在 `.gitignore` 中。如果你创建的临时目录不在 `artifacts/` 下（例如 `_debug/`、`tmp_render/`），请同步追加到 `.gitignore`。
- **可恢复的清理**：本地可以随时 `rm -rf artifacts/<topic>/` 重做，因为这些产物可由原始脚本重新生成。

判断标准：脚本是否会在后续开发周期被**反复运行**？

- 是 → 放在 `tools/`，可考虑提交
- 否 → 放在 `artifacts/<topic>/`，不提交

如果脚本里有值得复用的逻辑但当前不该提交（如某个 `inspect_*` 工具里有可通用的 helper），把 helper 提取到独立文件后再决定归属。

## 后续开发智能体工作方式

在本仓库继续工作时，开发智能体应：

1. 先查看本文件和现有 README、接口文档、设计文档。
2. 在修改代码前确认当前技术栈、目录结构和已有模式。
3. 对实时数据、图片流、算法结果、控制指令分别建模，不要混用临时字段。
4. 优先做可运行、可演示、可替换真实接口的版本。
5. 对任何控制相关逻辑保持保守，默认只读，除非用户明确要求并提供控制接口。
6. 修改完成后运行可用的测试、构建或浏览器验证，并在最终回复中说明验证结果。

